# Copy to .env (gitignored) and fill in. Never commit real values.
LIGASTAT_PIN=
# Needed for the "oracle" tests that verify app data directly against Sportmonks.
SPORTMONKS_API_TOKEN=
# Optional — defaults to the production Worker URL if unset.
# LIGASTAT_BASE_URL=https://sportmonks-friend-lookup.cumpelda.workers.dev
